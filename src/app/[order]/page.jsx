"use client"
// Import Components
import { useSearchParams } from "next/navigation";

const Order = () => {
    console.log(useSearchParams());
    return (
        <main>

        </main>
    )
}

export default Order;